class Solution:
    def removeElement(self, nums: List[int], val: int) -> int:
        s = len(nums) - 1
        while s >= 0:
            if nums[s] == val:
                nums.pop(s)
            s -= 1
        return len(nums) 
