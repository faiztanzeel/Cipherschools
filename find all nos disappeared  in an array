class Solution:
    def findDisappearedNumbers(self, nums: List[int]) -> List[int]:
        expect = set(i for i in range(1,len(nums)+1))
        actual = set(nums)
        
        return list(expect-actual)     
