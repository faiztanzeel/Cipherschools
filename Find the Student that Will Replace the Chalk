class Solution:
    def chalkReplacer(self, chalk: List[int], k: int) -> int:
        while k>=0:
            for j,i in enumerate(chalk):
                if k<0:
                    return j-1
                k = k-i
